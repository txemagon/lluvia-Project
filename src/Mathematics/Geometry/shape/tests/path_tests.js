assert( "Path#new. We can create a Path.",
        "a", "0",
        "a=0; try{;var b = new Path();}catch(e){i;a++;}" )

// assert( "Path#push. Pushing a line.",
//         "p.length", "1",
//         "p = new Path(); l = new StraightLine([1,2], [2,1]); p.push(l);"
//       )
